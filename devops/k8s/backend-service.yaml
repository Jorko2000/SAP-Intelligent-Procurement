apiVersion: v1
kind: Service
metadata:
  name: procurement-backend-service
spec:
  selector:
    app: procurement-backend
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
  type: ClusterIP
